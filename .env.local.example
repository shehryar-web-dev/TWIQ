# Base URL of your separate Node backend:
NEXT_PUBLIC_API_BASE=http://localhost:4000
# Optional bearer token if your backend requires one:
NEXT_PUBLIC_API_TOKEN=
# Use dummy fallback data when backend is unavailable: "1" or "0"
NEXT_PUBLIC_USE_DUMMY=1
